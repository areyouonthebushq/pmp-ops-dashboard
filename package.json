{
  "name": "pmp-ops",
  "private": true,
  "type": "module",
  "scripts": {
    "preflight": "node preflight.js",
    "test": "npm run preflight && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
